# Link extractor operations

```sh
./bin/scrapets linkextract --help
Usage: scrapets linkextract [OPTIONS]

  link extractor

Options:
  --file FILENAME   the path to the file for processing
  --directory PATH  the path to the directory for processing
  --filter TEXT     regular experssion for url filtering
  --help            Show this message and exit.
```
